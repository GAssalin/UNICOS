{
  "info": {
    "name": "ms-produtos - Histórico de Preço",
    "_postman_id": "c1e1-historico-preco-004",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Criar",
      "request": {
        "method": "POST",
        "url": "{{baseUrl}}/v1/historico-precos/produto/1",
        "header": [{ "key": "Content-Type", "value": "application/json" }],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"precoAnterior\": 99.90,\n  \"novoPreco\": 120.00,\n  \"motivo\": \"Reajuste\"\n}"
        }
      }
    },
    { "name": "Buscar Por ID", "request": { "method": "GET", "url": "{{baseUrl}}/v1/historico-precos/1" } },
    { "name": "Listar Todos", "request": { "method": "GET", "url": "{{baseUrl}}/v1/historico-precos" } },
    { "name": "Listar Por Produto", "request": { "method": "GET", "url": "{{baseUrl}}/v1/historico-precos/produto/1" } },
    { "name": "Listar Últimos 10", "request": { "method": "GET", "url": "{{baseUrl}}/v1/historico-precos/produto/1/ultimos" } },
    { "name": "Excluir", "request": { "method": "DELETE", "url": "{{baseUrl}}/v1/historico-precos/1" } }
  ]
}
